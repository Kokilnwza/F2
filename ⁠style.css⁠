/* ==========================================================
   SUSHI POP! DESIGN SYSTEM - NEO-BRUTALISM PLAYFUL & BOLD
   ========================================================== */
@import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800;900&family=Prompt:wght@300;400;600;700;800&display=swap');

:root {
  --bg-cream: #FFFDF5;
  --bg-peach: #FFD8CC;
  --pastel-orange: #FF884B;
  --pastel-yellow: #FFDE59;
  --accent-pink: #FF9AA2;
  --ink-black: #121212;
  --pure-white: #FFFFFF;
  --border-thick: 3.5px solid var(--ink-black);
  --box-shadow-main: 6px 6px 0px var(--ink-black);
  --box-shadow-hover: 10px 10px 0px var(--ink-black);
  --box-shadow-active: 2px 2px 0px var(--ink-black);
  --radius-playful: 24px;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Prompt', 'Plus Jakarta Sans', sans-serif;
  background-color: var(--bg-cream);
  color: var(--ink-black);
  line-height: 1.6;
  overflow-x: hidden;
  background-image: radial-gradient(var(--ink-black) 0.75px, transparent 0.75px);
  background-size: 24px 24px;
}

/* Typography */
h1, h2, h3, .nav-brand {
  font-family: 'Plus Jakarta Sans', 'Prompt', sans-serif;
  font-weight: 900;
  text-transform: uppercase;
  letter-spacing: -0.02em;
}

h1 {
  font-size: clamp(2.8rem, 7vw, 5.5rem);
  line-height: 1.05;
}

h2 {
  font-size: clamp(2rem, 4.5vw, 3.4rem);
  line-height: 1.15;
}

p {
  font-size: 1.1rem;
  font-weight: 400;
}

/* Common UI Components */
.container {
  max-width: 1240px;
  margin: 0 auto;
  padding: 0 24px;
}

.badge {
  display: inline-block;
  padding: 6px 16px;
  background: var(--pastel-yellow);
  border: var(--border-thick);
  box-shadow: 3px 3px 0 var(--ink-black);
  font-weight: 800;
  font-size: 0.9rem;
  border-radius: 999px;
  margin-bottom: 16px;
  transform: rotate(-2deg);
}

.btn-pop {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  background: var(--pastel-orange);
  color: var(--ink-black);
  font-weight: 800;
  font-size: 1.1rem;
  text-decoration: none;
  padding: 14px 32px;
  border: var(--border-thick);
  border-radius: 16px;
  box-shadow: var(--box-shadow-main);
  transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  cursor: pointer;
}

.btn-pop:hover {
  transform: translate(-3px, -3px);
  box-shadow: var(--box-shadow-hover);
  background: var(--pastel-yellow);
}

.btn-pop:active {
  transform: translate(2px, 2px);
  box-shadow: var(--box-shadow-active);
}

.btn-secondary {
  background: var(--pure-white);
}

/* Pop Box / Card */
.pop-card {
  background: var(--pure-white);
  border: var(--border-thick);
  box-shadow: var(--box-shadow-main);
  border-radius: var(--radius-playful);
  padding: 32px;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
  position: relative;
}

.pop-card:hover {
  transform: translateY(-5px);
  box-shadow: var(--box-shadow-hover);
}

/* Abstract Shapes */
.shape-blob {
  position: absolute;
  z-index: -1;
  border: var(--border-thick);
  pointer-events: none;
}

.shape-star {
  clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
  background: var(--pastel-yellow);
  width: 70px;
  height: 70px;
}

/* Header & Navbar */
.site-header {
  position: sticky;
  top: 16px;
  z-index: 100;
  padding: 0 20px;
  margin-bottom: 40px;
}

.nav-inner {
  max-width: 1240px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: var(--pure-white);
  border: var(--border-thick);
  box-shadow: var(--box-shadow-main);
  border-radius: 999px;
  padding: 12px 28px;
}

.nav-brand {
  font-size: 1.6rem;
  text-decoration: none;
  color: var(--ink-black);
  display: flex;
  align-items: center;
  gap: 8px;
}

.nav-brand span {
  color: var(--pastel-orange);
}

.nav-links {
  display: flex;
  list-style: none;
  gap: 20px;
  align-items: center;
}

.nav-links a {
  text-decoration: none;
  color: var(--ink-black);
  font-weight: 700;
  font-size: 1rem;
  padding: 8px 18px;
  border-radius: 999px;
  transition: all 0.2s ease;
}

.nav-links a:hover,
.nav-links a.active {
  background: var(--pastel-peach);
  border: 2px solid var(--ink-black);
  box-shadow: 2px 2px 0 var(--ink-black);
}

/* Marquee Ribbon */
.marquee-ribbon {
  background: var(--pastel-yellow);
  border-top: var(--border-thick);
  border-bottom: var(--border-thick);
  overflow: hidden;
  white-space: nowrap;
  padding: 12px 0;
  margin: 60px 0;
  transform: rotate(-1.5deg) scale(1.02);
}

.marquee-content {
  display: inline-block;
  animation: marquee 16s linear infinite;
  font-weight: 900;
  font-size: 1.4rem;
  text-transform: uppercase;
}

@keyframes marquee {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

/* Hero Section */
.hero-grid {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 40px;
  align-items: center;
  padding: 40px 0 60px 0;
}

.hero-collage {
  position: relative;
}

.collage-base {
  width: 100%;
  height: 420px;
  object-fit: cover;
  border-radius: var(--radius-playful);
  border: var(--border-thick);
  box-shadow: var(--box-shadow-hover);
}

.collage-tag {
  position: absolute;
  top: -20px;
  right: -20px;
  background: var(--pastel-orange);
  color: #fff;
  border: var(--border-thick);
  border-radius: 50%;
  width: 110px;
  height: 110px;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  font-weight: 900;
  font-size: 1rem;
  box-shadow: 4px 4px 0 var(--ink-black);
  transform: rotate(12deg);
  animation: float 3s ease-in-out infinite alternate;
}

@keyframes float {
  0% { transform: translateY(0px) rotate(12deg); }
  100% { transform: translateY(-10px) rotate(16deg); }
}

/* Cards Grid */
.cards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 32px;
  margin-top: 40px;
}

/* Footer */
footer {
  background: var(--ink-black);
  color: var(--pure-white);
  margin-top: 100px;
  padding: 60px 0 40px;
  border-top: var(--border-thick);
}

.footer-grid {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr;
  gap: 40px;
  margin-bottom: 40px;
}

.footer-col h4 {
  font-size: 1.3rem;
  margin-bottom: 16px;
  color: var(--pastel-yellow);
}

.footer-col ul {
  list-style: none;
}

.footer-col ul li {
  margin-bottom: 10px;
}

.footer-col a {
  color: #fff;
  text-decoration: none;
}

.footer-col a:hover {
  color: var(--pastel-orange);
  text-decoration: underline;
}

.footer-bottom {
  text-align: center;
  border-top: 1px dashed rgba(255,255,255,0.3);
  padding-top: 24px;
  font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 992px) {
  .hero-grid {
    grid-template-columns: 1fr;
    text-align: center;
  }
  .hero-actions {
    justify-content: center;
    display: flex;
  }
  .nav-inner {
    flex-direction: column;
    gap: 16px;
    border-radius: 24px;
  }
  .nav-links {
    flex-wrap: wrap;
    justify-content: center;
    gap: 8px;
  }
  .footer-grid {
    grid-template-columns: 1fr;
  }
}
